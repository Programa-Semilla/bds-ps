# Capital Semilla / FundingPlatform

Last updated: 2026-07-15

## Stack

.NET 10.0, ASP.NET MVC, EF Core 10, ASP.NET Identity, .NET Aspire, SQL Server (Aspire-managed container), Syncfusion HtmlToPdfConverter (Linux, vendored license fallback in dev), Tabler.io vendored CSS/JS, Playwright for E2E. Solution file: `FundingPlatform.slnx`.

## Layout

```text
src/
  FundingPlatform.AppHost          Aspire orchestrator — entry point for dev and tests
  FundingPlatform.Web              ASP.NET MVC UI, controllers, views, wwwroot
  FundingPlatform.Application      Use cases, projection services (e.g. IApplicantDashboardProjection)
  FundingPlatform.Domain           Entities, aggregates, domain events
  FundingPlatform.Infrastructure   EF Core DbContext, file storage, PDF generation, Identity wiring
  FundingPlatform.Database         dacpac — schema source of truth
  FundingPlatform.ServiceDefaults  Shared Aspire defaults (telemetry, health checks)
tests/
  FundingPlatform.Tests.Unit
  FundingPlatform.Tests.Integration
  FundingPlatform.Tests.E2E        Playwright + AspireFixture
specs/                             NNN-slug/ per feature: spec.md, plan.md, tasks.md
scripts/                           Verification scripts (asset budget, tokens, pdf carve-outs, perf baselines)
brainstorm/                        Working scratchpad for in-flight design exploration
```

## Run / build

- Dev (with persistent SQL data volume + auto-deployed dacpac):
  `dotnet run --project src/FundingPlatform.AppHost`
- Build whole solution: `dotnet build FundingPlatform.slnx`
- Schema changes: edit `FundingPlatform.Database` (dacpac). AppHost auto-deploys at startup outside ephemeral mode.

## Configuration knobs (read in `AppHost.cs`)

| Key | Default | Notes |
|---|---|---|
| `EphemeralStorage` | `false` | When `true`: skip persistent SQL data volume, skip auto-deploy of the SQL project, force sentinel admin password to `Sentinel123!`. Set by E2E fixture. |
| `Syncfusion:LicenseKey` | dev fallback embedded | Override in real envs. |
| `FundingAgreement:LocaleCode` | `es-CR` | Default culture. |
| `FundingAgreement:CurrencyIsoCode` | `CRC` | Funding-agreement currency (spec 015 — was `COP` before multi-currency). |
| `SignedUpload:MaxSizeBytes` | `20971520` (20 MiB) | Signed-PDF upload cap. |
| `AdminReports:DefaultCurrency` | `CRC` | Reports currency code; also pre-fills the supplier quotation Currency input. Spec 015: must match a code in the seeded `dbo.Currencies` catalog so the conversion path can resolve a rate. |
| `AdminReports:CsvRowLimit` | `50000` | Streaming CSV row cap. |
| `Admin:DefaultPassword` | (configured) | Sentinel admin password outside ephemeral. |
| `Storage:Provider` | `Azurite` (dev) / `AzureBlob` (prod) / `LocalFilesystem` (fallback) | Spec 014 — selects the `IObjectStorage` impl. Fail-fast in `Production` if `LocalFilesystem` is paired with a connection string. |
| `Storage:LocalFilesystem:RootPath` | `./.localstorage` | Required when `Provider=LocalFilesystem`. Host is responsible for encryption-at-rest (FR-026 — local provider does **not** provide it). |
| `Storage:Categories:{name}:MaxSizeBytes` | per-category default | Per-`FileCategory` upload cap enforced at the controller boundary by `UploadSizeGuard` (spec 014 / FR-021). |
| `Storage:Categories:{name}:UrlExpirySeconds` | `300` (5 min, max 900) | SAS URL TTL when `ServingMode=TimeLimitedUrl`. Hard cap is 15 min (FR-019). |
| `Storage:Categories:{name}:RetentionPolicy` | `none` | Future-seam string. `signed-funding-agreements` is the legal-hold candidate (FR-023). |
| `Storage:TestFallback:AllowFilesystem` | `false` | When `true`, the E2E `AspireFixture` may swap to `LocalFilesystem` if Azurite cannot start (FR-008). Logs a warning. |
| `Notifications:Provider` | `Mailtrap` (Local) / `Mailgun` (non-Local) | Spec 021 — selects the `IEmailSender` impl. Absence of provider config in non-Production → `NoOpEmailSender` with WARN log (FR-015). |
| `Notifications:BaseUrl` | per env | Absolute base URL used to compose CTA deep links in email bodies (FR-026). |
| `Notifications:NonProdAllowlist` | `["@programa-semilla.test"]` (dev) | Spec 021 / FR-017 — recipients whose email or `@domain` is not in the list are dropped and recorded as `BlockedByAllowlist`. Empty list is fail-closed. Bypassed in Production (FR-019). |
| `Notifications:Mailgun:ApiKey` / `Domain` / `BaseUrl` | `` / `` / `https://api.mailgun.net/v3` | Mailgun HTTP API config (FR-014). AppHost fails fast in Production when any of ApiKey/Domain/Sender:Email/BaseUrl is missing (FR-016). |
| `Notifications:Mailtrap:Host` / `Port` / `Username` / `Password` | from Aspire smtp4dev binding | SMTP path config; in Local resolved automatically from the Aspire smtp4dev sidecar endpoint. |
| `Notifications:Worker:PollIntervalSeconds` / `MaxAttempts` / `BatchSize` | `5` / `3` / `25` | `EmailDispatchWorker` poll cadence + retry budget + per-poll claim batch size (FR-003, FR-021). |
| `Notifications:Sender:Name` / `Email` | `Programa Semilla / Sistema de Banca para el Desarrollo` / `no-reply@programa-semilla.cr` | RFC-5322 From: display + address used by every variant (FR-014, spec 019 sender display). |

### Spec 021 sidecar

The Aspire AppHost registers `rnwood/smtp4dev` as a container resource named `smtp4dev` with two endpoints (`smtp` TCP 25, `http` 80). The Web project consumes both via `WithReference` so `MailtrapSmtpEmailSender` resolves the dynamic SMTP host:port; the E2E `MailCaptureClient` consumes the HTTP REST API.

## Testing

- Unit: `dotnet test tests/FundingPlatform.Tests.Unit`
- Integration: `dotnet test tests/FundingPlatform.Tests.Integration`
- E2E: `dotnet test tests/FundingPlatform.Tests.E2E`
- E2E uses `AspireFixture`, which boots AppHost with `--EphemeralStorage=true`. Each fixture run starts with a clean SQL Server container; the fixture deploys the dacpac via `sqlpackage` and waits synchronously before tests start.
- Sentinel admin in ephemeral E2E: `admin@programa-semilla.test` / `Sentinel123!`. Demo seeds: `applicant@programa-semilla.test`, `reviewer@programa-semilla.test`, `demo-admin@programa-semilla.test`, `auditor@programa-semilla.test` (role `Auditor`, spec 038; all `Demo123!`). All seed emails live in the `Notifications:NonProdAllowlist` default `["@programa-semilla.test"]` so mail captures in smtp4dev without further config.
- Integration tests must hit a real DB, never mocks. Mocks burned the team on a prod migration last quarter.
- Delivery bar: a feature is not delivered until the **corresponding/filtered E2E tests have been personally executed and are green** (filter to the test classes that exercise the change). Structural readiness or type-checks alone do not count. The **full** E2E suite is run only for critical/cross-cutting changes or on explicit request — not as the default gate (a full run takes ~30 min).

## Conventions

- Default culture: **es-CR**. Translation/localization is in scope (spec 012). UI copy should not be English-only.
- UI: Tabler.io vendored under `wwwroot/lib/`. Fonts (Fraunces / Inter / JetBrains Mono) and `canvas-confetti` also vendored. **No CDN — all static assets are local.**
- New managed (NuGet) dependencies require spec approval. Default posture: reuse what is vendored.
- PDF generation: Syncfusion needs a license key at runtime (`SyncfusionLicenseValidator`). The dev fallback in `AppHost.cs` lets local runs work; real envs must override.
- Speckit checkpoints: at every phase checkpoint, commit and push without prompting.
- UX/UI quality wins over E2E selector stability. HTML restructuring + E2E rewrites are in scope when elevating UI.

## Deploy to Azure

The `dev` environment lives in Azure subscription **LinaSys-DevEnv** (`d428f98f-…`), resource group `rg-CapitalSemilla-D`, region centralus, azd env name `CapitalSemilla-D`. Container App `webapp`. Custom domain: `https://capitalsemilla-dev.programasemilla.com/`.

### Deploy steps (in order)

1. **Code + container env:** `cd src/FundingPlatform.AppHost && azd up` (incremental — infra already provisioned). For a code-only redeploy: `azd deploy webapp`.
2. **Schema (NOT auto-deployed in publish mode):** the AppHost only registers the SqlProject in run mode, so Azure SQL is never updated by `azd up`. After every code deploy that changes schema, run `bash scripts/publish-dacpac-azure.sh`. The script handles dacpac build + transient firewall + AAD token. Defaults are destructive (`BlockOnPossibleDataLoss=false`, `DropObjectsNotInSource=true`). **Always use `--no-drop` for this env** — otherwise sqlpackage drops the contained DB user `mi-negelwcexrtzc` (created by the `sqlserver-roles` bicep outside the dacpac) and the running app loses DB auth.
3. **Auth to deploy schema:** your AAD account needs DDL on `fundingdb`. Server AAD admin is normally `sqlserver-admin-negelwcexrtzc` (sid `e991a9d7-28bc-46d5-ba15-bf68559c8541`). If your `az` user isn't that admin, temporarily set yourself: `az sql server ad-admin update -g rg-CapitalSemilla-D -s sqlserver-negelwcexrtzc --display-name "<you>" --object-id "<your-objectId>"`, deploy, then restore with `--display-name sqlserver-admin-negelwcexrtzc --object-id e991a9d7-28bc-46d5-ba15-bf68559c8541`.
4. **Tag the deployed commit on `main`:**
   ```bash
   git tag -a deploy/<env>/<YYYY-MM-DD>[-<N>] -m "<env> environment release to Azure (...)"
   git push origin deploy/<env>/<YYYY-MM-DD>[-<N>]
   ```
   Convention: `deploy/<env>/<YYYY-MM-DD>` where `<env>` is `dev|staging|prod`. Suffix `-2`, `-3`, … when more than one deploy lands the same day. Tags are **annotated** and point at the merged-to-main commit that was built and shipped. List releases: `git tag -l 'deploy/dev/*'`.

### Things that bite during a fresh deploy

- `azd` does **not** forward azd-environment (`.env`) values into the AppHost's manifest-generation subprocess. Code that reads runtime business config via `builder.Configuration[...]` inside `if (IsPublishMode)` will see `null` and must not throw on absent values — runtime fail-fasts belong in the Web project, not the AppHost.
- The on-disk Container App template `infra/webapp.tmpl.yaml` is the source of truth for the deployed container env (and overrides the AppHost's `WithEnvironment` literals once `infra/` exists on disk). Per `next-steps.md`: drift here will silently outlast AppHost changes. Reference azd-env via `{{ .Env.X }}` for non-secrets and Container App secrets (`secretRef:`) for credentials, both sourced from azd-env.
- `azd deploy` has occasionally shipped a BuildKit-cache-stale image. If a fresh deploy ships missing files that are clearly in the Dockerfile, `docker builder prune` then redeploy.

### Alternative: single-VM fixed-cost deploy (`deploy/vm/`)

A second, **fixed-monthly-cost** deployment path that does not use azd/Container Apps. Azure has no native "stop at $X" (budgets only alert; Container Apps + Azure SQL bill by usage), so a single VM's fixed compute cost is the lever. One Linux VM runs everything via Docker Compose: Caddy (auto Let's Encrypt TLS for `capitalsemilla-dev.programasemilla.com`) + the webapp (built on the VM from `src/FundingPlatform.Web/Dockerfile`) + SQL Server 2022 container; attachments go to Azure Blob via the VM's managed identity (cheap, durable), with LocalFilesystem as a fallback. Logs stay on the VM (`docker compose logs`, or the optional in-memory Aspire Dashboard) — **zero Log Analytics cost**. Full runbook: `deploy/vm/README.md`.

One-time provision (from dev machine, needs `az`): `deploy/vm/provision-vm.sh` (creates the RG + VM + NSG) → `deploy/vm/provision-storage.sh` (Blob account + managed-identity grant) → point DNS at the printed IP → create `deploy/vm/.env` on the VM from `.env.example`.

Deploy / update (idempotent, from dev machine) — `deploy/vm/deploy.sh` rsyncs the repo to the VM (never touching the VM's `.env`), rebuilds the image on the VM, recreates only what changed:

```bash
./deploy.sh <VM IP>                                  # push a code update
MSSQL_SA_PASSWORD='…' ./deploy.sh <VM IP> --schema   # also publish the dacpac
./deploy.sh <VM IP> --no-build                       # recreate without rebuilding
./deploy.sh <VM IP> --logs                           # tail webapp+caddy after
```

Schema-only publish (also used by `--schema`): `deploy/vm/publish-dacpac-vm.sh <VM IP>` (dacpac over an SSH tunnel; SQL is loopback-only on the VM). Note: the `dev` Azure resource group was deleted on 2026-06-01 to stop a Log Analytics billing spike, so this VM path is the current forward plan rather than the azd stack.

## Specs

`specs/NNN-slug/` is the source of truth for feature intent — spec.md, plan.md, tasks.md, and contracts. Read the spec before changing behavior in that area. Specs span 001-core-model-submission through 043-regulatory-freshness-hacienda-sync. **Number collisions — disambiguate by slug:** two `021-` (`021-email-notifications`, `021-feedback-session-may13`) and two `041-` (`041-email-brand-lift`, `041-evidence-inbox`).

<!-- MANUAL ADDITIONS START -->

<!-- MANUAL ADDITIONS END -->

## Managed dependencies (each added one required spec approval per Conventions)

- `Anthropic.SDK` NuGet — AI quote comparison (`IAiClient`), approved via spec 020 (A-10).
- MailKit v3 (MIT) — SMTP `IEmailSender` path, spec 021. Note: spec 021-feedback-session-may13's `SmtpEmailSender` deliberately uses `System.Net.Mail.SmtpClient` instead (no MailKit, per its NFR-005).
- smtp4dev (`rnwood/smtp4dev`) — Aspire container resource in Local only; Mailgun HTTP API is the non-Local provider (spec 021).
- Azurite — Docker container for blob storage in dev+test; Azure Blob in prod, local-filesystem fallback (spec 014).

Per-spec architectural seams (interfaces, aggregates, tables) are summarized in **Recent Changes** below and detailed in each `specs/NNN-slug/plan.md`.

## Recent Changes
- 045-financial-disbursement-core (financial-execution program **slice P1 of 9**, **PR #78**): The money-execution spine downstream of an executed funding agreement. A group-scoped **Financial Operator** records **Disbursements**, attaches a typed **bank receipt + invoice**, and a pure `DisbursementReconciliation.Evaluate` compares the three amounts **to the colón** (zero tolerance, all discrepancies **blocking**): disbursement↔receipt, disbursement↔invoice, Σ↔allocation. Balances project from an **append-only ledger** (`DisbursementLedgerEntry`, entry types `Allocation`/`Disbursement`) **plus** mutable off-ledger pending disbursements → five dimensions (Allocated/Paid/Validated/Pending/Available; `Available = Allocated − Paid`, may go **negative** as an over-disbursement signal, never clamped). Disbursements are **freely correctable until an explicit `Validar`, then locked**; `Validar` re-runs the over-disbursement check against the freshly-read committed Σ (the race-proof gate single-row `RowVersion` can't catch). **New model** — `Disbursement`/`DisbursementEvidence`/`DisbursementLedgerEntry` standalone aggregates keyed by `ApplicationId` (036 precedent), 5 `: byte` TINYINT enums (**`HasConversion<byte>()`** gotcha), pure evaluator + `ReconciliationDiscrepancy`/`ParticipantBalance` VOs. **Allocation** = the executed app's CRC rollup reusing `ApplicationCurrencyTotal.Compute`, snapshotted once into the ledger at first record via the shared `DisbursementAllocation.ResolveAsync` (same figure for the reconciliation ceiling **and** the projected `Allocated`). `IDisbursementService`/`IParticipantBalanceProjection` + `DisbursementService` (two-SaveChanges audit, blob-first evidence via the 036 storage stack, best-effort blob cleanup, `RowVersion` concurrency). **New group-scoped `Financial Operator` role** (reuses Auditor 038/040 machinery); **Auditor + Admin are read-only** (FR-025 — money movement is the operator's segregated duty; narrowed from the plan's R10 "FinOp+Admin" write set, confirm-on-review). `DisbursementController` at `Applications/{id}/Disbursements` (group-overlap + executed-state **flat 404 no-disclosure**, cross-app id guard, `[UploadSizeGuard]` + `EvidenceFileTypePolicy` magic-byte gate) + `DisbursementInbox` reusing spec-041 `IEvidenceInboxProjection`; es-CR `DisbursementResources`. `disbursement.*` `AdminAuditEvent` family (before/after payload). 3 additive dacpac tables (**filtered-unique ledger indexes**: one Allocation/app, one Disbursement/validated-disbursement) + `10_SeedFinancialOperatorRole.sql`; **no new managed deps; additive dacpac-only schema**. **Deep review** (5 agents): 0 Critical, 9 Important + 9 Minor **all fixed** (real bug: `RecordAsync` now catches the unique-index `DbUpdateException` on the concurrent first-Allocation race) — `specs/045-financial-disbursement-core/review-findings.md`. Spec/Plan/Code gates 100% (`REVIEW-{SPEC,PLAN,CODE}.md`). Unit **753/0**, Integration **473/0**, filtered E2E **10/0** (`DisbursementReconciliation`/`ParticipantBalance`/`DisbursementPartialAndOver`/`DisbursementLifecycle`/`DisbursementRoleScoping`). **Slices P2–P9 parked** (tranches & budget-lines, evidence graph, full reconciliation engine, currency, interest/fees/refunds/reversals, reporting, segregation of duties, migration) + multi-agency/Mentori — resumable roadmap in `brainstorm/41-financial-disbursement-platform.md`. Also carried: an app-wide solid-`.badge.bg-secondary` contrast fix (text was invisible under the brand theme) in `tokens.css`. PR #78.
- 044-process-reception-windows (feedback-3 slice E, **PR #77**): Replaced the per-application **Solicitud duration** submission gate with admin-configured **absolute-date reception windows** on a Process. Submission **and** new-draft creation are gated on "now ∈ an active window" — a pure UTC-instant comparison; the Costa Rica timezone is applied **only** at admin input/display via a new `IBusinessTimeZone`/`BusinessTimeZone` (config `Process:BusinessTimeZone`, default `America/Costa_Rica`, fixed −06:00 `TimeSpan` fallback). Revisión/Facturación timing untouched. **No new managed deps; dacpac-only schema.** (1) **New model** — `ProcessEvent` entity + `dbo.ProcessEvents` table as a *general* event store (`ProcessEventType` `ReceptionWindow=0` today; `Informational`/`Deadline`/`Milestone` seams — **TINYINT `HasConversion<byte>()` gotcha**), FK→Processes NO ACTION, `CK_ProcessEvents_EndAfterStart`, `RowVersion`. Pure `ReceptionWindowEvaluation.Evaluate(windows, nowUtc)` (empty→Unrestricted / inside→Open w/ **latest-End wins** on overlap / else NextWindow→BeforeFirst|Between / else AllWindowsClosed) with `ReceptionWindowSnapshot`/`SubmissionAvailabilityStatus`/`ReceptionAvailability` (`CanSubmit`/`CanCreateDraft`) + `ReceptionWindowClosedException` (carries `Status`+`BoundaryUtc`). (2) **Legacy gate removed** — dropped `Process.SolicitudWindowDays` and *every* consumer: `Application.Submit(int minQuotations)` (no more `currentStage/stageClosesAt/now` args or `StageWindowClosedException`), the `AutosaveFieldHandler` Solicitud throw (FR-015: existing-draft editing always allowed), and the `StageExpiryEvaluator` Solicitud arm (Revisión/Facturación kept). Idempotent post-deploy `07_DropSolicitudWindowDays.sql` (`COL_LENGTH` guard) + `SeedData.sql` `Stage.Solicitud.WindowDays` row removal. No-window ⇒ submission **open** (FR-007/SC-005). (3) **US1 admin CRUD** — `IReceptionWindowService`/`IReceptionWindowQuery` (`GetForProcessAsync`/`GetAvailabilityForGroup|ApplicationAsync`); reception-window create/edit/activate/deactivate/delete on `/Admin/Processes/{id}` with state badges (Upcoming/OpenNow/Closed) + `end>start` validation. (4) **US2–US5 applicant** — submission + new-draft creation gated; typed es-CR refusal (`ReceptionWindowResources`); applicant countdown notice partial `_ReceptionWindowNotice` on Details **and** the `/Applications/{code}/Review` submit surface (deep-review FINDING-1: the "Confirmar y enviar" button is the real submission point — now reception-aware + disabled with the timing reason); draft-guard. **Deep review** (5 agents): 0 Critical, 7 Important **all fixed** (submit-surface awareness, navigational-POST 422→redirect, bogus Draft "Revisión" banner, untested TZ conversion, …), 16 Minor (9 fixed / 7 documented) — `specs/044-process-reception-windows/review-findings.md`. Spec/Plan/Code gates at 100% (`REVIEW-{SPEC,PLAN,CODE}.md`). Unit **743/0**, Integration **462/0** (real SQL), filtered E2E `ReceptionWindow*` **9/9** (`ReceptionWindowAdminTests`+`ReceptionWindowApplicantTests`). PR #77.
- 043-regulatory-freshness-hacienda-sync (feedback-3 slice D, **PR #75**): Closed the slice-A (038) compliance loop. (1) **Staleness gate** — an application cannot advance through the audit stage (generate/confirm/release) while any *selected* supplier (`Item.SelectedSupplierId`, research D2) has a stale/never-reviewed required field (Hacienda/CCSS/SICOP; window `Regulatory:FreshnessWindowDays`=30; `Supplier.IsRegulatoryStale`/`StaleRequiredFields`, pure). Server-side at `FundingAgreementController.Generate` (non-admin auditor path) + defense-in-depth in `AuditWorkflowService.ConfirmPdfAsync`/`ReleaseForSignatureAsync` (new `RegulatoryStaleRefusalAsync`); es-CR message names provider+field+last-reviewed via new `UserFacingErrorCode.RegulatoryDataStale` (translator passes Detail verbatim, mirrors `SupplierCcssSinInscripcion`); re-authorize (slice-A "Reviewed — No Change") clears it. Backed by `IRegulatoryFreshnessService`/`RegulatoryFreshnessService` (selected-supplier distinct query). (2) **Daily Hacienda sync** — `HaciendaSyncService : BackgroundService` (shared `DailyRunSchedule` at `RunAtLocalTime` CR + public `RunOnceAsync` seam) over config-gated `IHaciendaApiClient` (`Live` `LiveHaciendaApiClient` manual long-lived HttpClient / `Fake` `FakeHaciendaApiClient` — **offline-default in DI** so the live API is never hit in dev/E2E; mirrors `AiComparison` Stub-default), pure `HaciendaStatusMapper` (research D1: 404→`SinInformacion` ≠ 200 `No inscrito`→`SinInscripcion`; `DesinscritoDeOficio` never auto-set), reuses slice-A audit verbs (source `Api`), **attributes audit/`LastReviewedBy` to the real system-sentinel id** (the `"system"` literal violated `AspNetUsers` FKs — InMemory hid it, E2E caught it), per-provider `RowVersion` skip (FR-025) + failure-isolated batch (FR-024). 3 nullable `dbo.Suppliers` columns (`HaciendaSyncAttemptAt/Outcome/Error`, `HaciendaSyncOutcome` enum, EF `HasConversion<byte?>`). (3) **Failures visible** — per-provider outcome on supplier detail + "verificación fallida" admin-list filter/badge (`SupplierAdminFilter.HaciendaSyncFailed`) + `supplier.hacienda_sync_failed` audit; failure never corrupts data (FR-018). (4) **Warning + digest** — non-blocking `RegulatoryFreshnessWarning` ViewComponent on `Review.cshtml`/`Audit/Detail.cshtml`; daily group-scoped auditor digest sent **directly** via `RegulatoryFreshnessDigestService` (no outbox, no new `NotificationEvent`, research D3) through the spec-041 brand shell. Dev-only `/Dev/RunHaciendaSync`/`StageHaciendaOutcome`/`RunFreshnessDigest` seams for deterministic E2E. Shared es-CR copy in **Application** `RegulatoryFreshnessCopy` (spec-034 cross-layer precedent; Web `RegulatoryFreshnessResources` delegates). No new managed dep, no new `ApplicationState`/`NotificationEvent`; dacpac-only schema. Deep review (5 agents): 0 Critical/security, 6 Important fixed, 7 Minor documented (`specs/043-.../review-findings.md`). Unit 727/0, Integration 446/0, filtered E2E 7/7 (`RegulatoryFreshnessBlock`/`HaciendaSync`/`RegulatoryFreshnessWarningDigest` + slice-C `AuditorWorkflow` regression). **Known follow-up:** FR-025 RowVersion-skip verified by construction only; in-process scheduler skips a day if the host is down at the run time (research-D4 design). PR #75.
- 042-audit-email-brand-lift: Brand-lifted the two spec-040 auditor-workflow emails (`SentToAuditAuditor`, `ReturnedToReviewerFromAudit`) that were authored pre-redesign and missed the 041 merge — they rendered inside the branded shell but had raw `<h1>`/`<p>` bodies + a bare `<a>` link, and `.text` twins with the stale "Sistema de Banca para el Desarrollo" sign-off. All 4 views (html+text) rewritten to compose the brand partials (`_Hero`/`_StatusCard`/`_CtaButton`) + inline-CSS Inter paragraphs + ALIA naming (FR-007); findings still render as a styled list; `.text` twins now use the `EmailBrand` constants. New E2E `AuditNotificationsBrandTests` drives the real reviewer→audit→return ceremony, captures both emails, and brand-asserts each (logo, partner-footer, absolute image host not `localhost:5000`, teal CTA, support phone, ALIA, no stale sign-off, findings present) — closing the mail-capture coverage gap that let plain-body audit emails ship. View-only; no schema/DI change, no new deps. E2E `AuditNotificationsBrand` 1/0, `EmailTemplateSender` brand sweep 22/0, Web build clean. PR #74.
- 041-email-brand-lift: ALIA transactional-email brand UI-lift (PR #73). Rebuilt the deliberately text-only email layout (spec 021 FR-023/NFR-001, intentionally reversed) into one Programa Semilla design system — a 600px table-based, inline-CSS `_EmailLayout` shell + shared partials (`_BrandHeader`/`_PartnerFooter`/`_CtaButton`/`_StatusCard`/`_DetailList`/`_Hero`), brand teal palette, hosted logo + 5-partner footer strip from `wwwroot/lib/brand/` (no new static path, no dacpac change, no new deps). **All** in-scope emails route through the one shell: the ~20 spec-021/028 **outbox** templates AND the spec-021/033 **direct-send** identity/stage/supplier templates, via a generalized `IEmailViewRenderer` (extracted from `RazorEmailRenderer`; replaces plain-text token substitution — direct-send `EmailMessage` gained optional `TextBody`, `SmtpEmailSender` ships `multipart/alternative`; the three identity factories moved Singleton→Scoped). ALIA platform naming (sign-off stays "Equipo Programa Semilla"), voseo copy, support phone +506 4600-1234, in-sync `.text.cshtml` twins (text path HTML-decodes the layout-less render so `@`-expressions show literal accents/phone). **New `ApplicationUnderReviewApplicant` outbox event** (enum **20**) at the real `Submitted→UnderReview` transition — `Application.StartReview()` in `ReviewService` writes a `VersionHistory("StartReview")` row to anchor the `(EventType,ApplicationId,VersionHistoryId,RecipientUserId)` dedup key (OQ-2: distinct from the submission receipt); `ReviewService.GetApplicationForReviewAsync` gained a `reviewerUserId` param. **New `PasswordChangedEmailFactory`** direct-send confirmation at every reset/change success point (`ConsumePasswordResetTokenHandler` incl. spec-033 invite first-set + `Account`/`Profile` ChangePassword; best-effort, never blocks). **OQ-1 resolved → withdrawn**: the "Nueva empresa para revisión" notifier stub was a duplication of spec 038's supplier→auditor notification (`IProviderCreatedNotifier`, fired from `SupplierCatalogService.CreateDraftWithBranchAsync`); deleted — no auditor group-scoping (auditors stay platform-wide for that path). **Image-host bugfix**: image (logo/partner-strip) + outbox CTA URLs were always built from the static `Notifications:BaseUrl`, while account links used a request-aware fallback — so a stale/dev base rendered broken `http://localhost:5000/lib/brand/*.png` images even though links worked. New `IEmailBaseUrlProvider` (Application abstraction; `EmailBaseUrlProvider` Web impl + `AddHttpContextAccessor`) centralizes the precedence (configured absolute base outside Development → live request host → config fallback for the no-request dispatch/stage-reminder workers); all 6 composers (`RazorEmailRenderer` + Invitation/ForgotPassword/PasswordChanged/StageReminder factories + `ProviderCreatedNotifier`) now use it. AppHost pins the dev `Notifications:BaseUrl` to the Web app's own Aspire external endpoint when unset (deployed VM already sets `https://${APP_DOMAIN}`). **Merge note:** landed after PR #72, so spec-040's `ReturnedToReviewerFromAudit`/`SentToAuditAuditor` were renumbered **21/22** (event types are string-stored — no DDL). Unit (Email+Notifications) 53/0 (+`EmailBaseUrlProviderTests` precedence matrix), Integration 9/0 (`ProviderCreatedNotifier`/`ForgotPasswordEnumeration`/`StageExpiryReminder`), filtered E2E mail-capture green (outbox image is now asserted absolute + never `localhost:5000`). Spec: `specs/041-email-brand-lift/` (Evolution Log: FR-013 withdrawn, OQ-1/2/3 resolved). PR #73.
- 041-evidence-inbox: Closed the spec-036 follow-on gap where, after an agreement is executed, the application falls off **every** reviewer list, leaving the funds-usage evidence stage only transiently reachable. Two thin slices over existing seams — **no new `ApplicationState`, no schema change, no new managed dep** (`ProcessStatus Active=0/Closed=1` already EF-mapped, so no TINYINT-conversion work). **(1) Evidence inbox**: new `IEvidenceInboxProjection`+`EvidenceInboxRowDto` (Application/`EvidenceInbox/`) and EF impl `EvidenceInboxProjection` (Infrastructure/`Persistence/`, mirrors `ReviewerDashboardProjection` + `SignedUploadRepository.GetPendingInboxAsync`): one query — `State==AgreementExecuted` ∧ `Group.Process.Status==Active` ∧ group-overlap (admin short-circuit via `UserGroupMemberships`, empty-group non-admin → empty early-return), with `ExcludeDeleted`+`ExcludeArchivedFund`, ordered `UpdatedAt` desc, capped 200 (NFR-001 scoping in-query). `APP-{id:D5}` + "Solicitante" fallback formatted in-memory (not SQL-translatable). New `EvidenceInboxController` (`[Authorize(Roles="Reviewer,Admin")]`, `GET /Evidence`) resolves scope via `IReviewerScopeProvider`; `Views/EvidenceInbox/Index.cshtml` table (`evidence-inbox-row`+`data-application-number`, `evidence-inbox-open`→`/Applications/{id}/Evidence`, `evidence-inbox-empty`); sidebar entry `evidence-inbox` in `operativoEntries` (Reviewer,Admin); es-CR `EvidenceInboxResources`. **(2) Process-close read-only gate** on the existing `FundsUsageEvidenceController`: private `IsProcessClosedAsync` (live `Group→Process.Status`, nullable-projected so null nav ⇒ read-write); `Index` sets new `IsReadOnly` VM flag; `Upload`/`EditNote`/`Delete` reject-with-toast (`Error_ProcessClosed`) when closed — **placed strictly AFTER `IsAccessibleAsync`/`EvidenceBelongsAsync`** so an unauthorized caller still gets the flat 404 and never learns "closed vs nonexistent" (FR-008), covering crafted POSTs (FR-007). `Index.cshtml` hides the upload form + shows `evidence-readonly-notice`; `_EvidenceRow.cshtml` renders the note read-only + drops delete (download always stays, FR-006). Read-only freeze applies to admins too (group bypass governs visibility only). Reachability confirmed: `AgreementExecuted` does **not** block `ProcessService.CloseAsync` (blocking states = Draft/Submitted/UnderReview/AppealOpen). **Deviations** (in `specs/041-evidence-inbox/tasks.md`): T006 InMemory-in-Integration (spec-036 precedent; real-SQL via E2E), optional T020 unit test skipped (redundant with T006), reopen-edge E2E skipped (live `Status` read already proven), US2 read-only assertions consolidated into one E2E method to share the isolated-process setup. Unit **685/0**, Integration **429/0** (+9 `EvidenceInboxQueryTests`: state×process-status×group + soft-deleted/archived-fund + admin), filtered E2E `EvidenceInbox` **4/0** (US1 list+open+upload & empty; US2 close→de-list+read-only+download+crafted-POST-rejected; US3 out-of-group-not-listed+404 & applicant-no-entry+refused), spec-036 `FundsUsageEvidence` regression **4/0** (SC-005). Also carried on-branch: spec-040 audit-surface reuse refactor (`ReviewDocumentsController`/`ReviewApplicationViewModelMapper`/`_ItemReviewDetails`/`audit-checklist.js`) + `ReviewerQueueProjection` now surfaces `ReturnedFromAudit` apps on the reviewer queue.
- 040-auditor-workflow-stage (feedback-3 slice C): Inserted a mandatory **auditor gate** between reviewer completion and the agreement reaching the applicant for signature. Two new `ApplicationState` ints — `PendingAudit=7`, `ReturnedFromAudit=8` — bracket the generate step; **release returns to `ResponseFinalized`** so the signing ceremony runs literally unchanged (D1). The reviewer's "Generate agreement" became **"Send to audit"** (gated on a reviewer checklist on `Review.cshtml`, shown at `ResponseFinalized && no agreement`); **PDF generation moved reviewer→auditor** — `FundingAgreementController.Generate` re-gated to **Auditor||Admin** (reviewer branch removed; non-admin path requires `PendingAudit` + a complete audit checklist via `IAuditWorkflowService.IsAuditChecklistCompleteAsync`; auditor reuses the existing PDF pipeline and is redirected back to `/Audit/{id}`). **Deviation:** `CanUserGenerateFundingAgreement` left unchanged (reviewer-removal enforced at the controller, not the widely-called domain helper, to avoid cascading red across signing); `CanGenerateFundingAgreement` broadened to accept `PendingAudit`. New domain transitions on `Application` (`SendToAudit`/`ReturnFromAudit`/`ResendToAudit`/`ReleaseForSignature` — each **returns its `VersionHistory`** anchor) + `CanAuditorGenerateFundingAgreement(bool auditChecklistComplete, out errors)` + `ConfirmAgreementPdf`; `FundingAgreement` gains `AuditorConfirmedAtUtc/ByUserId` + `ConfirmByAuditor` (cleared by `Replace`). Checklists = admin-configured per-stage templates (`ChecklistStage Reviewer|Auditor|Both`, **one active per stage value**, global; seed one `Both` default via `07_SeedChecklistTemplates.sql`): 3 new tables (`ChecklistTemplates`/`ChecklistTemplateItems`/`ApplicationChecklistResponses` — responses **snapshot item text** + FK NO ACTION per FR-003) + 2 nullable `FundingAgreements` columns; **no new enum DDL**. **Gotcha:** the `AppliesToStage`/`Stage`/`Status` enum columns are `TINYINT` → EF needs `HasConversion<byte>()` or real-SQL materialization throws `Byte→Int32` (InMemory hid it; E2E caught it). **Gotcha 2:** `StatusVisualMap.For(ApplicationState)` is an exhaustive switch that threw on the new states (Review status pill 500) — added both. **Gotcha 3:** template-edit full-replace **deactivates** old items (`ChecklistTemplate.DeactivateItems`) instead of hard-deleting — a referenced-item delete FK-violates the NO ACTION response FK (FR-003). `AuditWorkflowService` (Infrastructure) orchestrates send/audit/approve/confirm/release/return with the two-phase outbox save (mirrors `ReviewService.SendBackAsync`); `AuditorQueueProjection` reuses `IApplicationRepository.GetByStateForReviewerAsync` so the inbox is **group-scoped exactly like the reviewer queue**; `AuditController` (`[Authorize(Roles="Auditor,Admin")]`, group-overlap `Forbid()`) + `Views/Audit/*`; auditor read reuses `ReviewService` projection (slice-A provider compliance). **Auditors are now group-scoped** (FR-017): `NormalizeGroupIdsForRole` keeps Auditor memberships (only Admin groupless — supersedes spec-021 FR-007 for this role), admin user form shows the group selector for Auditor, sidebar gains an "Auditoría" inbox entry. 2 new notifications (`ReturnedToReviewerFromAudit=21`→reviewer bucket; `SentToAuditAuditor=22`→**new group-scoped Auditor `RecipientBucket`**, role `AUDITOR` ∩ applicant stage groups; values shifted from 20/21 when 041-email-brand-lift merged its enum-20 event) + 4 es-CR templates; existing `AgreementGeneratedApplicant` **re-pointed** off generation onto `ReleaseForSignatureAsync`. Admin checklist CRUD via `IChecklistTemplateService`/`ChecklistTemplateService` (mirror `FundService`; `checklist.*` audit) under `/Admin/Checklists`. Workflow audit = `VersionHistory`; checklist-admin audit = `AdminAuditEvent`. **No new managed deps.** Unit 682/0, Integration 418/0, filtered E2E 20/0 (`AuditorWorkflow`/`ReviewerSendToAudit`/`AuditReturn`/`ChecklistTemplateAdmin` + rewired `FundingAgreement`/`SigningWayfinding`/`GenerateAgreementQueue`). Deviations logged in `specs/040-auditor-workflow-stage/tasks.md`.
- 037-applicant-companies: Replaced the free-text **Empresa solicitante** on application creation with a controlled dropdown of admin-assigned companies (greenfield — nullable FK, no backfill, no new deps). New `Company` aggregate (`dbo.Companies`: one applicant→many, `Name`≤200, `ArchivedAt` soft-archive + filtered-unique active name `UX_Companies_ApplicantId_Name`, RowVersion) + nullable `Applications.CompanyId` FK (inline, NO ACTION) with the existing `CompanyName` column **re-purposed as a frozen name snapshot** — copied at creation, re-copied on draft re-select, frozen at submit, so renaming a company never rewrites prior applications (FR-016). `Application` ctor → `(applicantId, groupId, int? companyId, companyName)` (nullable for test/legacy rows; ~100-call-site compile-driven sweep), `SetCompanyName` made private, new `SetCompany` guarded by **`State==Draft`** (FR-015 immutable-after-submit). **US2 admin**: `ICompanyAdministrationService`/`CompanyAdministrationService` (mirrors `FundService`) add/rename/archive/unarchive under `/Admin/Users/{id}/Companies/...` (last-active-floor, accent-insensitive active-name uniqueness pre-check via `CompanyNameNormalizer` + filtered-index race backstop caught in `TrySaveAsync`→es-CR; idempotent archive); create-with-≥1 attaches in `UserAdministrationService.CreateUserAsync` (2nd SaveChanges + compensation that removes the orphan Applicant + user on failure — NO-ACTION FK gotcha); Edit-page "Empresas" card; `company.*` audit (`company.` prefix in `AdminAuditEventWriter`). **US1 applicant**: `CreateApplicationCommand` carries `CompanyId`; `ApplicationController` 0/1/many selector (spec-029 anchor pattern + spec-031 `data-searchable`) + server-side ownership/active validation via `ICompanyRepository.GetActiveByIdForApplicantAsync`→`CompanyInvalid` (no disclosure, FR-018/019). **US3**: draft re-select autosaved under new field-key `CompanyId` (`AutosaveFieldHandler`; `autosave.js` now binds `change` for `<select>`); FR-020 archived-at-submit gate in `SubmitApplicationHandler`. **US4**: batch CSV gains required trailing `Nombre de la empresa` (Count 10→11). Company error es-CR centralized in `IUserFacingErrorTranslator` (single source of truth). E2E bootstrap `/Account/SeedUser` seeds 2 companies; spec-018 free-text `CompanyNameApplicationFlowTests` replaced by `ApplicantCompanySelectionTests`. Multi-agent deep review applied (8/8 Important fixed; see `specs/037-applicant-companies/review-findings.md` + `EVOLUTION.md`). Unit 625/0, Integration 396/0, filtered E2E 17/17 (`ApplicantCompanySelection`/`AdminCompanyManagement`/`CompanyHistoryPreservation`/`BatchUserCreate`/`US2_ApplicantE2E`) + affected-class regression green. PR #68.
- 036-funds-usage-evidence: New post-disbursement reviewer stage — **Evidencia de uso de fondos** — where in-scope reviewers/admins upload/annotate(≤250-char note)/download/delete evidence files on an `AgreementExecuted` application. New `FundsUsageEvidence` aggregate (`dbo.FundsUsageEvidence`, FK→Applications/AspNetUsers NO ACTION, CK FileSize>0, RowVersion) with a domain factory enforcing the `AgreementExecuted` gate + ≤250 note invariant; new `FileCategory.FundsUsageEvidence` (container `funds-usage-evidence`, 20 MiB, BackendStream) + `StorageCategoryOptions`; `AdminAuditEvent.FundsEvidence{Uploaded,NoteEdited,Deleted}` + `funds_evidence.` prefix routing in `AdminAuditEventWriter`; pure `EvidenceFileTypePolicy` (PDF/PNG/JPEG/WebP/HEIC/Word/Excel magic-byte allow-list); `IFundsUsageEvidenceService`/`FundsUsageEvidenceService` (mirrors `FundService`; folds DB access in, no separate repo) — **gotcha**: the Infrastructure impl lives in `Infrastructure/Services/` (namespace `…Services`, NOT a `…FundsUsageEvidence` sub-namespace) to avoid a type-vs-namespace clash with the domain entity; **gotcha 2**: upload commits row+audit in two `SaveChanges` (no explicit transaction) — `AddSqlServerDbContext` enables the retrying execution strategy, which forbids a raw `BeginTransactionAsync`, so this matches the shipping `FundService` pattern. Reviewer-scoped `FundsUsageEvidenceController` under `/Applications/{id}/Evidence` (role + group-scope + executed-state gates — applicant→**403** role-refusal, out-of-group/pre-execution→**404** no-disclosure, plus a cross-app evidence-id guard; concurrent delete catches `DbUpdateConcurrencyException` → harmless); `Views/FundsUsageEvidence/Index.cshtml` + `_EvidenceRow.cshtml` + a conditional reviewer-only stage link on the executed funding-agreement panel; es-CR `FundsUsageEvidenceResources`. Reuses `IObjectStorage`/`IReviewerScope`/`UploadSizeGuard`/`IAdminAuditEventWriter`; E2E reuses `FundingAgreementSeeder.SeedExecutedAgreementAsync`. **No new state, no new deps.** 12/12 FR compliance; multi-agent deep review applied (see `specs/036-funds-usage-evidence/review-findings.md`). Unit 28/0, Integration 8/0 (+per-category oversize), filtered E2E 4/4 (`FundsUsageEvidenceTests` US1–US4). PR #65 (squash `2f5121f`).
- 035-line-item-category-templates: Reshaped the applicant submission flow (greenfield — no migration; no new deps). (1) **Admin-configured category field sets** — each `Category` owns a field set (`CategoryField` → item-keyed `CategoryFieldValue`, mirroring the impact-template EAV) rendered dynamically, replacing `Item.TechnicalSpecifications`. (2) **Application-level multi-impact + per-item attribution** (impact model **evolved 2026-06-16** away from the originally-built per-item `Item.ImpactTemplateId` design): impact data is collected at the **application level, one or more impacts** (`ApplicationImpact` ApplicationId→ImpactTemplate; `ImpactParameterValues` re-keyed to `ApplicationImpactId`), each line item **attributed** to ≥1 declared impact (`ItemImpact` join; `ApplicationImpactId` FK NO ACTION to avoid multi-cascade) + a required `Item.ImpactJustification NVARCHAR(300)`; impact templates come from any active template (Plantilla gating removed — keeps min-quotations + required-field flags). App-level **Impactos** card on Details/Review/reviewer surfaces (FR-011/SC-004), AI-assisted justification, `_DynamicFieldInputs` partial moved to `Shared`. (3) **Quotation reuse** within an application (share supplier + `Document`, per-item price; no schema change — reference-counted blob retention). Also: **scoped name uniqueness** (Process unique per-Fund / Group unique per-Process) + compound admin page titles disambiguating repeated names; Categories admin relabeled **"Plantillas de Categorías"** (sidebar + page title) with ImpactTemplates look&feel + name/description/status filtering. Surfaces show app-level declared impacts + per-item category values + attributed impact name(s) + justification. Spec: `specs/035-line-item-category-templates/` (spec.md Evolution Log, plan.md, research.md D13–D16, data-model.md, contracts/interfaces.md). PR #63 (squash `dd85313`).
- 034-batch-user-create: Admin-only **CSV bulk provisioning** of ≤200 Solicitante accounts under `/Admin/Users/Batch` (GET form + `Batch/Template` download + POST process), reusing the single-create + spec-033 invitation seams — **no schema change, no new managed deps**. Pure pieces in **Application** (`Admin/Users/Batch/`): in-house RFC-4180-subset `CsvParser` (quoted fields, `""` escape, embedded commas/newlines, CRLF/LF, BOM, blank-line drop), `PhoneNormalizer` (split on `/ , ; |` → first token → strip non-digits → drop leading `506` when >8 digits; whitespace is NOT a separator so "506 8888 1111"→"88881111"), `BatchUserCsvColumns` (canonical es-CR header `Grupo,Proceso,Fondo,Nombre,Apellido 1,Apellido 2,Email,Teléfono,Cédula,Código de usuario` + accent/case-insensitive BOM-tolerant `HeaderMatches` + `MaxDataRows=200`), transient DTOs (`BatchUserImportRow`/`BatchUserCreateOutcome`/`BatchUserCreateResult`), and **`BatchUserRowReasons`** (es-CR per-row reasons live in Application, NOT `Web.Resources`, because the Infrastructure service produces them — deviation from tasks T002 to respect Clean Architecture). Orchestration `IUserAdministrationService.CreateUsersBatchAsync` in **Infrastructure** `UserAdministrationService`: per row → required-cell + shape validation (one reason, first wins) → **`IdentificationInference.TryInfer`** (type inferred from the value: 9 digits→cédula física, 11–12→DIMEX, letters→pasaporte; 10-digit/other→errored — evolved 2026-06-12 from "fixed cédula física" after the first real client file carried DIMEX numbers) → **`EmailNormalizer.FirstEmail`** (multi-address cells keep the first) → in-file dedupe (email/canonical-cédula/código, first-occurrence-wins → "duplicado en el archivo") → spec-029 chain resolve (preloaded globally-unique Fund/Process/Group names; `group.ProcessId==process.Id && process.FundId==fund.Id`; unknown-name vs chain-mismatch distinguished) → `CreateUserAsync` (per-row atomic; `DomainError.Code`→es-CR reason). **Web**: `AdminUsersController` `Batch`/`BatchTemplate`/`Batch` POST (FR-003 file-level rejection — not-csv/byte-cap(1 MiB)/header-mismatch/empty/>200 rows → first es-CR message, create nothing; then issue+best-effort send the spec-033 invitation per created row), `AdminUserBatchViewModels`, `Batch.cshtml`/`BatchResult.cshtml` (succeeded/errored report), "Crear por lote" entry on the users list. Reason strings are es-CR; invitation only delivers to allowlisted recipients (E2E uses `@programa-semilla.test`). Unit 37/0 (`CsvParserTests`,`PhoneNormalizerTests`,`IdentificationInferenceTests`,`EmailNormalizerTests`), Integration 6/0 (`BatchUserCreationTests`: all-valid, id-inference+first-email, mixed, wrong-chain, unknown-names, template header), filtered E2E 5/0 (`BatchUserCreateTests`: AllValid+invitations captured, Mixed, ChainMismatch, HeaderMismatch, TooManyRows).
- 033-user-invite-email: Replaced the spec-032 admin-typed temp password with an emailed **72h single-use set-password invitation**. Admin create drops the password field; the account is created with **no password** + `MustChangePassword=false` (`CreateAsync(user)` no-password overload). The `AdminUsersController.Create` POST issues a 72h invite token, composes the absolute `/Account/ResetPassword` link, **direct-sends** an es-CR invitation email (`InvitationEmailFactory` + `Views/Emails/Identity/InvitationEmail.cshtml`, the `ForgotPasswordEmail` pattern — **not** the spec-021 outbox), and renders an **"Invitación enviada"** confirmation (`InvitationSent.cshtml`, `data-testid="invitation-sent"`) with a **copyable link** shown once (FR-008 delivery-resilience fallback; only the token hash is stored). A **resend** action (`POST /Admin/Users/{id}/ResendInvitation` + "Reenviar invitación" row action) supersedes the prior unused link. Reuse-first: the password-reset seam gained `Ttl` + `InvalidatePriorUnused` on `IssuePasswordResetTokenCommand`, `InvalidateUnusedAsync` on `IPasswordResetTokenStore`, and `PasswordResetToken.InvitationLifetime = 72h`. **Two expiry gates**: the per-row `ExpiresAt` (invite 72h / forgot-password unchanged at 60 min) AND the global Identity `DataProtectionTokenProviderOptions.TokenLifespan`, bumped 60min→**72h** in `Program.cs` (the stricter row TTL keeps forgot-password at 60 min; password-reset is the only DataProtector-token consumer, so the bump is side-effect-free — research D2). **No schema change, no new deps.** Cross-cutting E2E ripple (echoes spec 032 D-2): admin-created users now have no password and create lands on the confirmation (not a list redirect), so create-then-login tests onboard via new `SetPasswordViaInviteAsync`/`OnboardAndLoginAsync` base helpers (the latter uses the dev `LatestPasswordResetLink` seam), and post-create assertions target `InvitationSentPage`; the two obsolete first-login change-password tests were rewritten to the invite flow. Unit 529/0, Integration 355/0 (+`PasswordResetToken.InvitationLifetime` unit; +72h-TTL & `InvalidateUnusedAsync` supersede integration in `PasswordResetTokenStoreTests`), filtered E2E 46/0 (`UserInvitationTests` + rewired `AdminUserLifecycleTests`/`AdminUserCodeTests`/`AdminUserGroupAssignmentTests`/`ReviewerScopeTests`/`GroupDeletionCascadeTests`/`ConfirmDialogAndToastTests`/`InputMaskIdentificationTests`/`SentinelImmutabilityTests`/`AdminResetPasswordTests`/`UserCodeSearchTests`).
- 032-admin-user-code: Closed public self-registration + admin-assigned **unique applicant `UserCode`**. (1) Registration removed: deleted `AccountController.Register` GET/POST, `Register.cshtml`, `RegisterViewModel` → `/Account/Register` GET 404 (POST surfaces as 405 under the http→https redirect — see `tasks.md` D-5); register links stripped from home hero CTA (→ Login), login page, and the navbar "Crear cuenta" in `_Layout`. (2) New `Applicant.UserCode` (`NVARCHAR(50) NULL` on `dbo.Applicants` + filtered unique index `UX_Applicants_UserCode WHERE [UserCode] IS NOT NULL`; migration-safe nullable add, **no** post-deploy backfill; EF config mirrors it; entity trims/≤50 guard). Required for the **Solicitante** role at the controller boundary (mirrors required-LegalId); unique via a service pre-check (`USER_CODE_IN_USE`, mirrors `LEGAL_ID_IN_USE`) + the index backstop (`DbUpdateException`→es-CR in `AdminUsersController`). Threaded through `Create/UpdateUserRequest`, `UserDetailDto`, `UserSummaryDto`, the admin Create/Edit VMs+views (role-toggle JS, shown only for Solicitante) and `ProfileViewModel` (read-only on `/Profile`, gated by a new `IsApplicant` flag — note profile is attribute-routed at `/Profile`, not `/Account/Profile`). (3) Widened the single search box to also match identification + UserCode: admin users list (`ListUsersAsync` correlated `Applicants.Any(LegalId/UserCode)` sub-query, FR-012), reviewer queue + `QueueRows` (`+UserCode +Email` LIKE, FR-013), and Applications/Applicants/Aging reports + applicants CSV (`+UserCode` LIKE, FR-014); "Código de usuario" column on the admin list + applicants report/CSV (FR-016); es-CR placeholders. **Cross-cutting test-infra deviation (D-2):** removing public registration broke the E2E bootstrap (`AuthenticatedTestBase.RegisterUserAsync`, ~103 call sites) — replaced with a Development-only `GET /Account/SeedUser` dev seam (mirrors the existing `AssignRole`/`AssignAllGroups`/`ResetAdminFixture` seams; 404 outside Development; product surface stays admin-only). 3 direct `RegisterPage` consumers rewritten + page object deleted; `AdminUserCreatePage.FillAsync` auto-fills a unique UserCode for Applicant so legacy admin-create-applicant callers stay green. No new managed deps. Unit 527/0, Integration 352/0 (+`UserAdministrationUserCodeTests` 9, `ApplicantUserCodeTests` 5 in Unit), filtered E2E 30/0 (`RegistrationRemovedTests`, `AdminUserCodeTests`, `UserCodeSearchTests` + rewired `AuthenticationTests`/`InputMaskIdentificationTests`/`RoleAwareSidebarTests`). Full E2E (cross-cutting bootstrap): 324 pass / 5 skip, after fixing 4 additional registration-form-bound tests the `RegisterPage` grep missed (CSV header, RequiredMarker, HintTooltip→removed, EnumExclusion→relocated); the one remaining failure (`FilterToolbarSearchTests.FundFilterLevel`, spec-031 admin cascade-filter) is **pre-existing on the branch base** (`1b87745`) and untouched by 032 (D-6). FR-002 nuance: GET `/Account/Register`→404, POST→405 (D-5, left as-is per decision).
- 031-searchable-dropdowns: Type-to-filter autocomplete over data-driven dropdowns via one in-house vanilla `wwwroot/js/searchable-select.js` enhancer (IIFE, ES5 house style, no build step, no new deps). Opt-in per `<select data-searchable>`; the native `<select>` stays authoritative (posted value) and is hidden **in place** (1px clip, never moved — moving it into a wrapper detached it mid-action and made native-driven `SelectOptionAsync` tests racy). Enhancement is threshold-gated (selectable-option count `>` `data-searchable-threshold` ?? global `7`) and re-evaluated on a per-select `childList` MutationObserver so cascade/AJAX option rebuilds refresh the combobox (FR-008); a document-level observer enhances AJAX-injected selects. Accent/case-insensitive es-CR matching (`NFD` + combining-mark strip via char-code regex + `toLocaleLowerCase('es')`). Must-pick-from-list: typed text only filters, blur reverts to the committed label (FR-003). WAI-ARIA combobox (role/`aria-expanded`/`aria-controls`/`aria-activedescendant` + a visually-hidden polite count region); keyboard ↑/↓/Enter/Esc/Tab. es-CR copy comes from markup (`SearchableDropdownResources` → body-level `data-searchable-*` default + optional per-control `data-searchable-placeholder`); **no Spanish literals in JS** (FR-010). The group drilldown's checkbox level (not a `<select>`) gets a separate in-place text filter inside `group-drilldown-selector.js` (always-on when groups render; preserves spec-016/029 accumulation/chips). `data-searchable` added to ~14 control sites across shared partials + edit forms; location cascade selects gained a `data-testid` mirroring their generated id. New `PageObjects/SearchableSelect.cs` E2E helper drives the combobox (`[data-testid="<src>-search"]`); below-threshold/non-enhanced controls keep using `SelectOptionAsync` (the 1px-in-place native makes this still work — see Deviations in `tasks.md`). No schema/DTO/route change. Gotcha: in the ephemeral seed every cascade-fund level is ≤7 (1 fund/1 process/3 groups), so US1/US2 E2E SQL-seed 8 throwaway Funds (and DELETE them in teardown — the shared fixture would otherwise push other suites' Fund selects over threshold); the location cantón level is naturally >7 (San José = 20). Delivery: filtered E2E green — `SearchableDropdowns` 7/7, `SupplierLocationCascade` 4/4 (enhanced cantón/distrito, no regression), edited-view sweep (ExchangeRates/QuotationEdit/RenameProcess) 16/16.
- 030-edit-process-name: Inline admin rename of a Process `Name` on `/Admin/Processes/{id}` (the one previously-immutable Process detail). Thin seam mirroring the spec-029 Fund-reassignment path: `RenameProcessCommand` → `IProcessService.RenameAsync` → existing domain `Process.Rename()` (trim/≤120/equal-name no-op, **no** Closed guard — rename allowed at any status incl. Closed, FR-002); new `AdminAuditEvent.ProcessRenamed` (`process.renamed`, payload `{processId, oldName, newName}`; no audit on a no-op, FR-006). `[HttpPost("{id:int}/Rename")]` mirrors `ChangeFund`, with an extracted `BuildDetailsViewModelAsync` so the inline-error path re-renders Details identically; duplicate surfaces as `DbUpdateException` via `UX_Processes_Name` → es-CR "Ya existe un proceso con ese nombre." (reused from Create). Gotcha: the action param is `string? newName` — a non-nullable `string` gets an implicit `[Required]` whose English message ("The newName field is required.") would override the es-CR required validation (FR-008). Inline Name card on `Details.cshtml` rendered for Active AND Closed. No schema change, no new deps. Unit 522/0, Integration 341/0 (+`ProcessRenameServiceTests`; duplicate path is E2E-only since EF InMemory does not enforce the unique index), full E2E 302/0/5 (+`RenameProcessTests`).
- 029-fund-entity: `Fund` (Fondo) aggregate above `Process` (`Fund → Process → Group → Application`). New `dbo.Funds` + required `Processes.FundId` + `Applications.GroupId` anchor FKs, **migration-safe** (DEFAULT(0) placeholder + post-deploy `05_Fund029Anchors.sql` backfill + FK add — mirrors `Groups.ProcessId`; declaring NOT NULL + inline-FK failed the publish on populated DBs, which rolled back the whole deploy → "Invalid object name 'Funds'"). Admin Fund CRUD + regulation PDF (spec-014 `FileCategory.FundRegulation`, `%PDF-` magic-byte, `UploadSizeGuard`) + audit (`fund.*`); applicant download (`FundRegulationController`); required Active-Fund selector + Fund column/filter + reassign on Process; authoritative `Application.GroupId` anchor captured at creation (0=block / 1=auto / ≥2=choose) making Plantilla + stage-window resolution deterministic (replaces the `FirstOrDefault` lookup); force-freeze of archived-Fund apps via `IApplicationQueryFilter.ExcludeArchivedFund` (null-tolerant LEFT-JOIN form) at every non-admin read site + controller-boundary & domain mutation guards (`FundArchivedException`); exact Fund filter/column on the three admin reports + CSV. es-CR throughout; no new managed deps. 21/21 FR compliance. Unit 515/0, Integration 337/0 (+`FundServiceTests`), full E2E 297/0/5 (+4 Fund E2E classes). PR #56.
_Older entries condensed to one line each — see `specs/NNN-slug/` for full detail._
- 028-post-resolution-notifications: 12 post-`Resolved` `NotificationEvent` values (applicant-response, appeal lifecycle, signing ceremony) on the spec-021 outbox pipeline; event-aware CTA (`CtaRouteTemplate`) + actor-exclusion; PR #38.
- 027-review-funding-ux: Eight reviewer/applicant funding-agreement UX refinements; PDF body unchanged (spec 018 minimalism); no schema change.
- 026-input-masks: Structured-field input masks for email, CR phone, CR identification (cédula física/jurídica, DIMEX, NITE, passport); completes spec 021 FR-013.
- 025-supplier-location-cascade: Full three-level Provincia → Cantón → Distrito cascade on the supplier-branch form (wires spec 021 FR-014 + adds Distrito).
- 024-toast-confirm-dialogs: One toast + modal-dialog system replacing TempData banners + native `alert`/`confirm()` across all pages/roles.
- 023-quotation-edit: In-place per-quotation Edit while `Draft` (Price/Currency/ValidUntil/SupplierBranchId); `ComparisonArtifact` invalidation on save; no schema change. Variance: spec names `ReturnedForChanges`, no such enum — gate is on `Draft`.
- 022-combined-release: 020 + 021 merged for joint PR; no behavioral changes; conflict log in `specs/022-combined-release/plan.md`.
- 021-feedback-session-may13: 26 May-13 stakeholder refinements (US1–US8) — Process/Plantilla/PublicCode/Impact-at-Application, supplier autocomplete + cascade, autosave/masks/submit-gating, profile + forgot-password, stage-expiry reminders, acompañamiento copy + landing.
- 021-email-notifications: First email subsystem — transactional outbox + BackgroundService dispatcher, six v1 events, es-CR templates, fail-closed allowlist, idempotency via `(EventType,ApplicationId,VersionHistoryId,RecipientUserId)`, smtp4dev capture sidecar.
- 020-ai-quote-comparison: AI per-item quote comparison → hash-keyed `ComparisonArtifact`; `extract→normalize→compare` behind `IComparisonOrchestrator`; single Anthropic `IAiClient`; PII redaction; rate/token caps; superscript citations.
- 017-admin-ux-facelift: `/Admin` capability-complete dashboard (4 KPIs + 9 capability cards + activity feed); 10-surface admin sweep; route normalization; schema unchanged.
- 016-user-groups: Group-scoped reviewer access — `Group` + `UserGroupMembership` + `AdminAuditEvent`, admin Groups CRUD, multi-select selector, EF group-overlap predicate on queue/inbox/detail auth.
- 015-multi-currency-quotes: Multi-currency supplier quotations (CRC base + USD), buy-rate snapshotting, agreement PDF conversion notes.

<!-- SPECKIT START -->
**Active plan: none in progress.** The financial-execution program's **P1 (`045-financial-disbursement-core`) shipped (PR #78)**. The resumable **9-slice roadmap** lives in `brainstorm/41-financial-disbursement-platform.md`; the next candidate is **P2 — tranches & budget-lines** (per-line attribution + the `Committed` balance dimension), not yet brainstormed. Multi-agency + Mentori parked entirely.

**Last shipped: 045-financial-disbursement-core** (PR #78) — full detail in Recent Changes above. Financial-execution program **P1 of 9** shipped; **P2–P9 parked** (see the roadmap in `brainstorm/41-financial-disbursement-platform.md`). Prior program: **feedback-3 slices A–E shipped** — A `038` (PR #69), B `039` (PR #70), C `040` (in PR #72), D `043` (PR #75), E `044` (PR #77).

**Specs not detailed in Recent Changes above:**
- `039-supplier-recommendation` (slice B, PR #70) — seven-criterion deterministic explainable supplier recommendation; CCSS `sin inscripción` hard block + per-item approval guard; two new required quote fields (delivery lead time + warranty as `TimeDuration`/`DurationUnit`).
- `038-auditor-provider-compliance` (slice A, PR #69) — Auditor role + enumerated provider regulatory statuses + audit trail/freshness + warnings.
- `037-brand-alignment` (PR #67) — Programa Semilla brand facelift (token-driven palette/logos/dark-teal sidebar/de-zebra tables/kebab actions/PDF re-tint; visual-only, no backend/schema change).
- `041-evidence-inbox` co-shipped `040-auditor-workflow-stage` in PR #72 (041 was stacked on the never-merged 040 branch; no standalone 040 PR).
<!-- SPECKIT END -->
