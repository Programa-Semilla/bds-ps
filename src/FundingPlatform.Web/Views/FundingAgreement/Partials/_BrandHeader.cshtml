@*
    Spec 018 / FR-001 — seedling brand header. Repeated on every page via the
    layout's `position: fixed` rule (R-001). Swap-one-file ergonomics per FR-018:
    replace `wwwroot/lib/brand/pdf/header-seedling.png` and the next render picks
    up the new asset with no code change.
*@
<div id="brand-header">
    <img src="lib/brand/pdf/header-seedling.png" alt="Programa Semilla" />
</div>
