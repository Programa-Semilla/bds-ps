@*
    Spec 018 / FR-002 — partner-logo composite footer (Banca para el Desarrollo
    SBD + CROCUS + nexo + Programa Semilla + 10 años badge baked into a single
    image). Repeated on every page via the layout's `position: fixed` rule
    (R-001). Swap-one-file ergonomics per FR-018: replace the PNG and the next
    render picks up the new strip with no code change. Per-partner edits are
    out of scope for v1 (composite re-cut externally).
*@
<div id="brand-footer">
    <div class="gold-divider"></div>
    <img src="/lib/brand/pdf/footer-partners-strip.png" alt="Banca para el Desarrollo SBD - CROCUS - nexo - Programa Semilla - 10 años" />
</div>
