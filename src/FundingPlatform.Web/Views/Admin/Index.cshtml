@model FundingPlatform.Web.ViewModels.Admin.AdminDashboardViewModel
@{
    ViewData["Title"] = "Panel de administración";
}

@await Html.PartialAsync("Components/_PageHeader",
    new PageHeaderViewModel(
        Title: "Panel de administración",
        Subtitle: "Indicadores de acción y todas las herramientas administrativas en un solo lugar."))

@await Html.PartialAsync("Components/_AdminDashboard", Model.Data)
