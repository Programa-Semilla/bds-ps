@{
    ViewData["DisableLayout"] = true;
}Convenio firmado retirado — Solicitud #@Model.Payload.ApplicationId

Hola, @Model.Recipient.DisplayName:

Te informamos que el solicitante @Model.Payload.ApplicantDisplayName retiró el convenio firmado pendiente de la solicitud #@Model.Payload.ApplicationId en @EmailBrand.PlatformName.

Detalle de la solicitud:
- Solicitante: @Model.Payload.ApplicantDisplayName
- Número de solicitud: #@Model.Payload.ApplicationId

Por el momento no hay nada que revisar; te avisaremos si el solicitante vuelve a subir una versión.

Ir a la bandeja de firma:
@Model.CtaUrl

@EmailBrand.SignOffClosing
@EmailBrand.SignOff

—
Para consultas: @EmailBrand.SupportEmail · @EmailBrand.SupportPhone
@EmailBrand.AutomaticMessageNote
