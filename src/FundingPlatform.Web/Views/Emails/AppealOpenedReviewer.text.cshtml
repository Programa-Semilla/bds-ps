@model EmailRenderModel
@{
    ViewData["DisableLayout"] = true;
}Nueva apelación abierta — Solicitud #@Model.Payload.ApplicationId

Hola, @Model.Recipient.DisplayName:

Te informamos que el solicitante @Model.Payload.ApplicantDisplayName abrió una apelación sobre la resolución de su solicitud en @EmailBrand.PlatformName.

Podés ingresar a la plataforma para revisar el caso y responder según corresponda a tu perfil.

Detalle de la solicitud:
- Solicitante: @Model.Payload.ApplicantDisplayName
- Número de solicitud: #@Model.Payload.ApplicationId

Ver la apelación:
@Model.CtaUrl

@EmailBrand.SignOffClosing
@EmailBrand.SignOff

—
Para consultas: @EmailBrand.SupportEmail · @EmailBrand.SupportPhone
@EmailBrand.AutomaticMessageNote
