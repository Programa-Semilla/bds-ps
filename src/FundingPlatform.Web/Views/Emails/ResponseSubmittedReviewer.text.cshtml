@model EmailRenderModel
@{
    ViewData["DisableLayout"] = true;
}El solicitante respondió la resolución — Solicitud #@Model.Payload.ApplicationId

Hola, @Model.Recipient.DisplayName:

Te informamos que el solicitante @Model.Payload.ApplicantDisplayName registró su respuesta a la resolución de la solicitud #@Model.Payload.ApplicationId en @EmailBrand.PlatformName.

Detalle de la solicitud:
- Solicitante: @Model.Payload.ApplicantDisplayName
- Número de solicitud: #@Model.Payload.ApplicationId

Revisá las decisiones aceptadas o rechazadas para continuar con el proceso.

Revisar la respuesta:
@Model.CtaUrl

@EmailBrand.SignOffClosing
@EmailBrand.SignOff

—
Para consultas: @EmailBrand.SupportEmail · @EmailBrand.SupportPhone
@EmailBrand.AutomaticMessageNote
