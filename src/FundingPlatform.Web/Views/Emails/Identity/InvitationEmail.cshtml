@model DirectEmailModel
@* Spec 041 / T017 — branded set-password invitation (reference copy #1). Rendered
   through the shared _EmailLayout; copy + variables supplied by InvitationEmailFactory. *@
@{
    ViewData["Title"] = Model.Subject;
}
@await Html.PartialAsync("~/Views/Emails/Shared/_DirectBody.cshtml", Model)
