@model DirectEmailModel
@* Spec 041 / US3 / T031 — branded password-changed confirmation (reference copy #2).
   No CTA (FR-005); support phone is in the shared footer. *@
@{
    ViewData["Title"] = Model.Subject;
}
@await Html.PartialAsync("~/Views/Emails/Shared/_DirectBody.cshtml", Model)
