@model DirectEmailModel
@* Spec 041 / T017 — branded password-reset email. Rendered through the shared
   _EmailLayout; copy + variables supplied by ForgotPasswordEmailFactory. *@
@{
    ViewData["Title"] = Model.Subject;
}
@await Html.PartialAsync("~/Views/Emails/Shared/_DirectBody.cshtml", Model)
