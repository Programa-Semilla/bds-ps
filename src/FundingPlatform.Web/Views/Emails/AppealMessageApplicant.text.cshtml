@model EmailRenderModel
@{
    ViewData["DisableLayout"] = true;
}Nuevo mensaje del revisor — Solicitud #@Model.Payload.ApplicationId

Hola, @Model.Recipient.DisplayName:

Te informamos que un revisor respondió en la apelación de tu solicitud #@Model.Payload.ApplicationId en @EmailBrand.PlatformName.

Podés ingresar a la plataforma para leer el mensaje y continuar la conversación.

Ver mi apelación:
@Model.CtaUrl

@EmailBrand.SignOffClosing
@EmailBrand.SignOff

—
Para consultas: @EmailBrand.SupportEmail · @EmailBrand.SupportPhone
@EmailBrand.AutomaticMessageNote
