@model DirectEmailModel
@* Spec 041 / T018 — branded T-72h stage-expiry reminder. Copy + variables from
   StageReminderEmailFactory; rendered through the shared _EmailLayout. *@
@{
    ViewData["Title"] = Model.Subject;
}
@await Html.PartialAsync("~/Views/Emails/Shared/_DirectBody.cshtml", Model)
