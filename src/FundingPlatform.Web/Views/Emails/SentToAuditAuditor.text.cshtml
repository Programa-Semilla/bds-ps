@model EmailRenderModel
@{
    ViewData["DisableLayout"] = true;
}Nueva solicitud en auditoría — Solicitud #@Model.Payload.ApplicationId

Hola, @Model.Recipient.DisplayName:

La solicitud de @Model.Payload.ApplicantDisplayName (#@Model.Payload.ApplicationId) fue enviada a auditoría en @EmailBrand.PlatformName. Revisá la documentación, completá la lista de verificación de auditoría y aprobá o devolvé la solicitud al revisor.

Abrir en auditoría:
@Model.CtaUrl

@EmailBrand.SignOffClosing
@EmailBrand.SignOff

—
Para consultas: @EmailBrand.SupportEmail · @EmailBrand.SupportPhone
@EmailBrand.AutomaticMessageNote
