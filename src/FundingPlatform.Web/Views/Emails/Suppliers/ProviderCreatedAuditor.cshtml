@model DirectEmailModel
@* Spec 041 / T019 — branded auditor "nuevo proveedor registrado" email (reference
   copy #8). Rendered through the shared _EmailLayout; copy + provider "Detalle"
   card supplied by ProviderCreatedNotifier. *@
@{
    ViewData["Title"] = Model.Subject;
}
@await Html.PartialAsync("~/Views/Emails/Shared/_DirectBody.cshtml", Model)
