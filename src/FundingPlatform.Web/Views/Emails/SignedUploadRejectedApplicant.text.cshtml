@{
    ViewData["DisableLayout"] = true;
}Tu convenio firmado requiere cambios — Solicitud #@Model.Payload.ApplicationId

Hola, @Model.Recipient.DisplayName:

Revisamos el convenio firmado que subiste para la solicitud #@Model.Payload.ApplicationId en @EmailBrand.PlatformName y requiere cambios antes de poder aprobarlo. Ingresá a la plataforma para ver el detalle de lo solicitado y subir una versión corregida.

Ver mi convenio:
@Model.CtaUrl

@EmailBrand.SignOffClosing
@EmailBrand.SignOff

—
Para consultas: @EmailBrand.SupportEmail · @EmailBrand.SupportPhone
@EmailBrand.AutomaticMessageNote
