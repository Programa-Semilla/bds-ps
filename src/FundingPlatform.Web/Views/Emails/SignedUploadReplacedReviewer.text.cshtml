@{
    ViewData["DisableLayout"] = true;
}Convenio firmado reemplazado — Solicitud #@Model.Payload.ApplicationId

Hola, @Model.Recipient.DisplayName:

Te informamos que el solicitante @Model.Payload.ApplicantDisplayName reemplazó el convenio firmado pendiente de la solicitud #@Model.Payload.ApplicationId en @EmailBrand.PlatformName.

Detalle de la solicitud:
- Solicitante: @Model.Payload.ApplicantDisplayName
- Número de solicitud: #@Model.Payload.ApplicationId

La nueva versión está lista para tu revisión en la bandeja de firmas.

Revisar convenio firmado:
@Model.CtaUrl

@EmailBrand.SignOffClosing
@EmailBrand.SignOff

—
Para consultas: @EmailBrand.SupportEmail · @EmailBrand.SupportPhone
@EmailBrand.AutomaticMessageNote
